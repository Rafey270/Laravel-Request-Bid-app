ngDescribe({
    name: 'Test requests component',
    modules: 'app',
    element: '<requests></requests>',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
